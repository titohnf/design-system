name: Sync Design Tokens

on:
  push:
    branches: [main]
    paths:
      - 'tokens.css'

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout design-system
        uses: actions/checkout@v4

      - name: Extract token block
        run: |
          awk '/\/\* DESIGN-TOKENS-START \*\//,/\/\* DESIGN-TOKENS-END \*\//' tokens.css > /tmp/token-block.txt
          echo "Isi blok yang akan disebar:"
          cat /tmp/token-block.txt

      - name: Sync to komunitastera
        run: |
          git clone https://x-access-token:${{ secrets.SYNC_TOKEN }}@github.com/titohnf/komunitastera.git /tmp/komunitastera
          cd /tmp/komunitastera
          python3 - <<'EOF'
          import re
          with open('app/globals.css', 'r') as f:
              content = f.read()
          with open('/tmp/token-block.txt', 'r') as f:
              block = f.read().strip()
          new_content = re.sub(
              r'/\* DESIGN-TOKENS-START \*/.*?/\* DESIGN-TOKENS-END \*/',
              block,
              content,
              flags=re.DOTALL
          )
          with open('app/globals.css', 'w') as f:
              f.write(new_content)
          EOF
          git config user.name "design-system-bot"
          git config user.email "bot@tera.or.id"
          git add app/globals.css
          git diff --cached --quiet || git commit -m "chore: sync design tokens from design-system repo"
          git push

      - name: Extract primary-only subset for foundation
        run: |
          grep -E '^\s*--primary:|^\s*--primary-dark:|^\s*--primary-foreground:' tokens.css > /tmp/primary-lines.txt
          python3 -c "
          lines = open('/tmp/primary-lines.txt').read().strip().split('\n')
          out = ['/* DESIGN-TOKENS-START */']
          for l in lines:
              k, v = l.strip().split(':', 1)
              k = k.replace('--', '--color-')
              out.append(f'{k}:{v}')
          out.append('/* DESIGN-TOKENS-END */')
          open('/tmp/foundation-block.txt', 'w').write('\n'.join(out))
          "
          echo "Isi blok khusus foundation:"
          cat /tmp/foundation-block.txt

      - name: Sync to foundation
        run: |
          git clone https://x-access-token:${{ secrets.SYNC_TOKEN }}@github.com/titohnf/foundation.git /tmp/foundation
          cd /tmp/foundation
          python3 - <<'EOF'
          import re
          with open('src/index.css', 'r') as f:
              content = f.read()
          with open('/tmp/foundation-block.txt', 'r') as f:
              block = f.read().strip()
          new_content = re.sub(
              r'/\* DESIGN-TOKENS-START \*/.*?/\* DESIGN-TOKENS-END \*/',
              block,
              content,
              flags=re.DOTALL
          )
          with open('src/index.css', 'w') as f:
              f.write(new_content)
          EOF
          git config user.name "design-system-bot"
          git config user.email "bot@tera.or.id"
          git add src/index.css
          git diff --cached --quiet || git commit -m "chore: sync design tokens from design-system repo"
          git push
